name: Test
on:
  pull_request:

env:
  GH_TOKEN: ${{ github.token }}
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: make install
      - run: gh activity-report --author srz-zumix -d 7..3
